{
  "name": "borg-chat-ai-client",
  "version": "0.0.1",
  "description": "A more fluent client for the OpenAI API",
  "author": "vim@sammuti.com",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "gts": "^3.1.1",
    "typescript": "~4.7.0",
    "@types/node": "^14.11.2"
  },
  "scripts": {
    "start": "npx tsc && node build/src/example.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
